package com.example.demo.repository;

import com.example.demo.model.Interet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InteretRepository extends JpaRepository<Interet, Integer> {
}
