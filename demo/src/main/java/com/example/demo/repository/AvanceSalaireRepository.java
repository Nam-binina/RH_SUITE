package com.example.demo.repository;

import com.example.demo.model.AvanceSalaire;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AvanceSalaireRepository extends JpaRepository<AvanceSalaire, Integer> {
}
